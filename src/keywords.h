#pragma once
#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <string>

using namespace std;

void run(string node, string command);
void surun(string node, string command);
void add_node(string node, string /*command*/);
void rm_node(string node, string /*command*/);
